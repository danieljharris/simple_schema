%%-*- mode: erlang -*-

{application, simple_schema, [
    {description, "Simple Json Schema Validation"},
    {vsn, git},
    {applications, [kernel,
                    stdlib,

                    jsn
                   ]}
 ]}.

% vim:ft=erlang
